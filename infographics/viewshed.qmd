---
title: "Viewshed"
image: "../images/infographic/map-viewshed-pts.jpg"
description: "How will wind turbines affect your view?"
editor_options: 
  chunk_output_type: console
---

::: panel-tabset
## Overview

```{r}
librarian::shelf(
  dplyr, glue, here, leaflet, readr)

d <- read_csv(
  here("infographics/viewshed_locations.csv"),
  show_col_types = F)

d %>% 
  leaflet() %>%
  addProviderTiles(providers$Esri.OceanBasemap) %>% 
  addMarkers(
    lng   = ~lon,
    lat   = ~lat,
    label = ~location,
    popup = ~glue("TODO: {region}, {location}"))
```

## Humboldt

```{r}
d %>% 
  filter(
    region == "Humboldt") %>%
  leaflet() %>%
  addProviderTiles(providers$Esri.OceanBasemap) %>% 
  addMarkers(
    lng   = ~lon,
    lat   = ~lat,
    label = ~location,
    popup = ~glue("TODO: {region}, {location}"))
```

## Morro Bay

```{r}
d %>% 
  filter(
    region == "Morro Bay") %>% 
  leaflet() %>%
  addProviderTiles(providers$Esri.OceanBasemap) %>% 
  addMarkers(
    lng   = ~lon,
    lat   = ~lat,
    label = ~location,
    popup = ~glue("TODO: {region}, {location}"))
```
:::
